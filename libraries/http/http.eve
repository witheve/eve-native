# HTTP

## Send an HTTP Request

search
  h = [#http/request address]
watch http
  ("request", h, address)
end

search
  response = [#http/response request]
  request = [#http/request]
commit
  request.response := response
end

## Process HTTP Requests

search
  h = [#http/server address]
watch http
  ("server", h, address)
end