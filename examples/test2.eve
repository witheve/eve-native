~~~ eve
search
  card = [#record-card/card]
  title = if av = card.av av.attribute = "name" then av.value else "Unitled"
bind
  card.title += title
end
~~~

~~~ eve
search
  card = [#record-card/card av]
  title = if av.attribute = "name" then av.value else "Unitled"
bind
  card.title += title
end
~~~
